language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-khulayeoman' ]; then cd .. && eval "mv $currentfolder generator-khulayeoman" && cd generator-khulayeoman; fi
